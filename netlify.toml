# netlify.toml
# هذا الملف يضمن أن جميع المسارات تُوجه إلى index.html في تطبيق React SPA

[build]
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200